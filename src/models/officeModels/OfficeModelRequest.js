class OfficeModelRequest {
    constructor(address, registryPhoneNumber, isActive) {
        this.address = address;
        this.registryPhoneNumber = registryPhoneNumber;
        this.isActive = isActive;
    }
}

export default OfficeModelRequest;
